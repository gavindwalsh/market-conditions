# Lens deploy targets — shared across all lens.avos.co sub-apps (§2).
# Values mirror avos-country-dashboard/infra/config.env (same bucket + distro +
# Cognito→Entra auth cover every /<slug>). Safe to commit — no secrets here.
# Source of truth for the AWS resources is that repo's infra/ setup scripts.
S3_BUCKET=lens-avos-061039759840-us-east-2-an
S3_BUCKET_REGION=us-east-2
CLOUDFRONT_DISTRIBUTION_ID=E4HQZ6QR9ZX9Z
DOMAIN=lens.avos.co
# Verified against avos-country-dashboard/infra/config.env 2026-07-08 — match.
